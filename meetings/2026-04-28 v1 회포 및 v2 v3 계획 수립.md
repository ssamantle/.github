# v1. As-Is

* 기본 Semantle 게임 기능 구현
* 실시간 멀티플레이 요소 추가
	* 커스텀 사용자명으로 게임에 참가
	* 지정된 시간에 동시에 시작하고, 동시에 종료
	* 정답을 별도로 지정할 수 있도록 추가
	* 레이스 맵 (실시간 리더보드) 기능 추가
	* 정답을 맞추면 Confetti 애니메이션 재생

## v1. TODO

* Server API 별 Throughput 측정하기
* Redis - RDB Cache Hit Rate 올리기
* MSA 적용하기
	* 임베딩 작업을 별도의 서비스로 분리하기
	* 서비스간 통신은 REST API
* HTTPs 로 호스팅 하는 선택지 제공
* Client 에 전파되는 변경사항이 없도록 할 것. (= 하위 호환성 유지)
* i18s → Korea Time Zone 기준 시간 표기하기.

# v2.

> [!warning] 무조건 비용 절감이 1순위.
> 우리는 자린고비 메타입니다.

* 사용자 인증 수단 추가
	* User : (Email - Password)
	* OAuth 2.0 (버전 확인 필요)
		* Google
		* GitHub (No 2 Factor)
	* Cookie + JWT
		* Refresh token 구현? ← 서버 자율
		* Scale out 시에 키 교환 어떻게? → Redis
		* 인증 서버 분리?

* 게임을 동시에 여러 개 호스트 가능
	* 게임 생성 권한 → 있어야만 생성 가능, 기본적으로는 생성 불가
	* 동시에 몇 개 까지?
	* 게임 로비 기능 구현

* 사용자 권한 관리
	* API 접근 권한 분리
	* 구현 방식
		* 사용자 Type 구분 + Flag 방식 → 어떻게? 관련 프레임웤 있나? 장고는 기본 기능인데
	* 권한 종류
		* 자기가 만든 게임 Update / End
		* ... 추가 회의 필요

* 실시간 동기화 API 관련
	* 기존 Polling 방식 유지? 대체한다면?
		* Socket 사용 유무
		* Long Polling 
		* Server Side Event

* Framework 유지?
	* Server (backend)
		* FastAPI (vs) **Spring Boot** (vs) Express.js
	* Client (frontend)
		* TypeScript 기반 React.js
	* Database (postgres, redis)
	* Embedding Model (model)
		* FastAPI
		* Throughput 성능 최적화
		* Caching 을 한다면 Hit rate 높이는 방법
		* 유사도와 단어 순위. → Pre-caculation 

* 로그 수집
	* 모든 데이터는 삭제 금지 (하더라도 soft-delete)
	* Cascade 금지
	* 데이터 수집 프레임 워크가 따로 필요한가?
		* 시각화 도구는? Kibana? ELK 스택? (ElasticSearch + Logstash + Kibana)
	* 모든 엔드포인트 수행시간 측정하기 → (ex: took 20ms for `/api/v2/guess` )
	* 로그 데이터만 가지고도 90% 이상 데이터 복구가 가능하도록 하기.
		* 로그 데이터 용량은 Max 10mb x 32 files
	* 로그는 볼륨(컨테이너 꺼져도 저장 가능하도록) → NoSQL
		* MongoDB 사용? 월별 파티션 아이디어 (by @NiceHCY)

* 배포/인프라 관련
	* 서버 Scale out 에 대비.
	* 배포 Friendly 한 구조
		* ssamantle-deploy 에서 `docker compose up` 만으로 모든 서비스를 완전 프로덕션 환경으로 배포할 수 있도록 하기.
		* 모든 개별 서비스는 도커 컨테이너화 하기.

* 성능 목표 수치 (KPI)
	* 1 게임에 동시에 1,023명 (싸피 전국 캠퍼스 기준)
	* 허용 최대 동시 접속자 수 65,535(= 1,024 x 64 - 1) 명
	* 최대 63 게임 동시 실행
	* 1인 당 허용 TPS
		* 1 game sync per second
		* Avg = , Worst =

* 기간 계획
	* 아무리 길어도 5월 안에 끝내기
		* 기능 구현을 2.5주 안에 끝내고
		* 1주는 테스트와 성능 개선
# v3. 
* 게임 모드 추가
	* 아이템 전 (상대방 n-th 답안 훔쳐오기, 입력 칸 강제 잠금)
* 서버리스 아키텍처로 전환
	* 유지비 0로 수렴시키기 목표
	* IaC 작성하기
* 데이터 분석
	* 월별 통계
	* 데이터 수집 파이프라인
	* Data Lake 설계
	* Data ETL
